kind = "web"
previewPath = "/"
title = "Kit Copa dos Craques"
version = "1.0.0"
id = "artifacts/kit-copa"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25826

[services.development]
run = "pnpm --filter @workspace/kit-copa run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/kit-copa", "run", "build" ]
publicDir = "artifacts/kit-copa/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25826"
BASE_PATH = "/"
